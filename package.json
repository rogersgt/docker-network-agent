{
  "name": "docker-network-agent",
  "version": "0.0.0",
  "description": "Detect devices on the network",
  "engines": {
    "node": "10.6.x"
  },
  "main": "src/index.js",
  "scripts": {
    "dev": "npm install && NODE_ENV=development node src",
    "start": "NODE_ENV=production node src"
  },
  "author": "Grady Rogers",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.3.3",
    "dotenv": "^6.2.0",
    "network": "0.4.1"
  }
}
